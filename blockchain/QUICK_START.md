# 🚀 Быстрый старт: Отображение NFT в TronLink

## ✅ Ваш контракт УЖЕ является TRC721!

Стандарт TRC721 в TRON **идентичен** ERC721. Ваш контракт `SGAA_NFT.sol` уже полностью совместим с TronLink.

## 🎯 Проблема: Почему NFT не отображается?

TronLink **не** отображает NFT автоматически. Нужно:
1. ✅ Настроить метаданные (tokenURI должен возвращать JSON)
2. ✅ Добавить NFT в TronLink вручную

## 📝 3 простых шага

### **Шаг 1: Создайте метаданные**

Создайте JSON файл и загрузите на IPFS или свой сервер:

```json
{
  "name": "SGAA NFT #1",
  "description": "Sale Google Ads Accounts NFT",
  "image": "https://your-domain.com/images/1.png",
  "attributes": [
    {
      "trait_type": "Type",
      "value": "Google Ads Account"
    }
  ]
}
```

**Варианты размещения:**
- 🔹 **IPFS** (рекомендуется): https://pinata.cloud или https://nft.storage
- 🔹 **Собственный API**: https://your-api.com/metadata/1.json

### **Шаг 2: Установите URI в контракте**

#### Вариант A: Через скрипт (автоматически)

```bash
# 1. Отредактируйте scripts/set_metadata.js
# 2. Укажите адрес контракта и базовый URI
# 3. Запустите:
node scripts/set_metadata.js
```

#### Вариант B: Через TronScan (вручную)

1. Откройте https://shasta.tronscan.org/#/contract/TT46M2bES5JrWLbVUGfZiCvVfz8aHGRypp/code
2. Перейдите на вкладку **"Write Contract"**
3. Подключите TronLink
4. Вызовите функцию **`setBaseURI`**:
   - Введите: `ipfs://QmYourCID/` или `https://your-api.com/metadata/`
5. Или вызовите **`setTokenURI`** для каждого токена:
   - tokenId: `1`
   - uri: `ipfs://QmYourCID/1.json`

### **Шаг 3: Добавьте NFT в TronLink**

1. Откройте **TronLink** кошелек
2. Перейдите в **"Collectibles"** или **"NFT"**
3. Нажмите **"+"** или **"Add NFT"**
4. Выберите **"TRC-721"**
5. Введите:
   - Contract Address: `TT46M2bES5JrWLbVUGfZiCvVfz8aHGRypp`
   - Token ID: `1` (или другой ID вашего токена)
6. Нажмите **"Add"**

## 🔍 Проверка

Используйте скрипт для проверки NFT и метаданных:

```bash
# Проверить контракт и токен #1
node scripts/check_nft.js 1

# Проверить баланс адреса
node scripts/check_nft.js 1 TYourAddress...
```

## 📚 Подробная документация

Смотрите `NFT_METADATA_GUIDE.md` для:
- Подробных инструкций по работе с IPFS
- Примеров кода для минта с метаданными
- Решения частых проблем
- Информации о формате метаданных

## ❗ Важные моменты

1. **Метаданные ОБЯЗАТЕЛЬНЫ** для отображения в TronLink
2. JSON должен быть **публично доступен** (через IPFS или HTTP)
3. Изображение тоже должно быть **доступно** по прямой ссылке
4. После установки URI **перезапустите TronLink**

## 🛠️ Полезные команды

```bash
# Проверить NFT
node scripts/check_nft.js 1

# Установить метаданные
node scripts/set_metadata.js

# Минт нового NFT (через interact.js)
node scripts/interact.js
```

## 💡 Почему другой контракт работает?

Контракт по адресу `TT46M2bES5JrWLbVUGfZiCvVfz8aHGRypp` работает, потому что:
- ✅ У него правильно настроены метаданные (tokenURI возвращает JSON)
- ✅ Метаданные доступны по URL
- ✅ Изображения доступны

Ваш контракт **ничем не отличается** - просто нужно настроить метаданные!

## 🎨 Не нужно менять код контракта!

Ваш контракт УЖЕ поддерживает все необходимое:
- ✅ `tokenURI(uint256 tokenId)` - есть
- ✅ `setTokenURI(uint256 tokenId, string memory uri)` - есть
- ✅ `setBaseURI(string memory baseURI)` - есть
- ✅ Все события и функции ERC721/TRC721 - есть

Просто установите метаданные и добавьте NFT в TronLink вручную!


