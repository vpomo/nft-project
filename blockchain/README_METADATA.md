# ❓ Структура JSON для метаданных NFT

## Короткий ответ:

**НЕТ, структура JSON НЕ произвольная!** 

Существует **стандарт метаданных NFT** (OpenSea Metadata Standard), который должен соблюдаться для корректного отображения в TronLink и других кошельках/маркетплейсах.

---

## ✅ Обязательные поля

```json
{
  "name": "Название NFT",           ← ОБЯЗАТЕЛЬНО
  "description": "Описание NFT",    ← ОБЯЗАТЕЛЬНО
  "image": "URL к изображению"      ← ОБЯЗАТЕЛЬНО
}
```

**Эти 3 поля ОБЯЗАТЕЛЬНЫ** для отображения NFT в TronLink!

---

## 🎨 Полная структура (с опциональными полями)

```json
{
  "name": "SGAA NFT #1",
  "description": "Sale Google Ads Accounts NFT",
  "image": "ipfs://QmYourCID/1.png",
  "external_url": "https://your-website.com",    ← опционально
  "animation_url": "ipfs://QmCID/video.mp4",     ← опционально
  "background_color": "FFFFFF",                  ← опционально (без #)
  "attributes": [                                 ← опционально
    {
      "trait_type": "Type",
      "value": "Premium"
    },
    {
      "display_type": "number",
      "trait_type": "Level",
      "value": 5
    }
  ]
}
```

---

## ❌ Что НЕ будет работать

```json
{
  "title": "My NFT",        ❌ Неправильно! Должно быть "name"
  "info": "Description",    ❌ Неправильно! Должно быть "description"
  "picture": "url"          ❌ Неправильно! Должно быть "image"
}
```

```json
{
  "name": "My NFT"          ❌ Неправильно! Отсутствуют description и image
}
```

---

## 📚 Примеры файлов

В папке есть примеры правильных JSON:

- **`metadata-minimal.json`** - минимальный JSON (только обязательные поля)
- **`metadata-example.json`** - полный JSON с атрибутами
- **`metadata-with-video.json`** - JSON с видео/анимацией

---

## 📖 Подробная документация

Читайте **`METADATA_STANDARD.md`** для:
- Подробного описания всех полей
- Примеров разных типов атрибутов
- Рекомендаций по изображениям
- Списка поддерживаемых форматов

---

## 🔍 Как проверить свои метаданные?

1. **Валидация JSON**: https://jsonlint.com/
2. **Проверка через скрипт**:
   ```bash
   node scripts/check_nft.js 1
   ```

---

## 💡 Итог

| Вопрос | Ответ |
|--------|-------|
| Может ли JSON быть произвольным? | ❌ **НЕТ** |
| Есть ли стандарт? | ✅ **ДА** (OpenSea/ERC721) |
| Какие поля обязательны? | ✅ `name`, `description`, `image` |
| Можно добавить свои поля? | ⚠️ Можно, но не отобразятся |
| Как добавить свойства? | ✅ Через массив `attributes` |

**Используйте стандартную структуру с обязательными полями `name`, `description`, `image`!**

Только так NFT корректно отобразится в TronLink! 🎯


